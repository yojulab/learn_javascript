"use strict";

// String Operators
"my" + "cat";
"1" + 2;
const strConcate = `string 1+2=${1 + 2}`;

// Numeric Operators
2 + 2;
2 - 2;
2 / 2;
2 * 2;
4 % 2;
2 ** 2;

// increment and decrement operatros
let counter = 1;
counter++;
++counter;

let number = 3;
counter += number;
counter -= number;
counter *= number;
counter /= number;
